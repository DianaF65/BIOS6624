################################################################################
###                                Project 4 Notes                           ###
################################################################################

### Questions
  - Is specifying a correlation of 0 equivalent to having all variables independent
  of each other? Is case 1a part of case 1b?
  - 

### Notes from Class

# 4/?
# Ex: Linear regression. 
# My professor says that the estimators for the regression coefficients are unbiased. 
How would I evaluate if that were true in practice for small, medium, and large
datasets?
# Sketch out the simulation plan
- What question is to be investigated?
   - What is the bias of the regression coefficients for varying sample sizes?
- Define the data generating mechanism and what factors to vary
  - We will generate data from the normal distribution with varying sample sizes
  
##### 4/27 Class Notes
- Goal by wednesday is to create shell of function needed for simulations
- How to set the correlation among the predicots in the simulation model
  - Exchangeable with settings 0, 0.35, and 0.7
  
#### 4/29 Class Notes
- Make sure you are writing for clinician or non-stats audience
- Use an alpha of 0.5 for this project
- Ridge > lasso when variables are correlated
- Paper in Week 12 for picking the N for simulations 
  - Table that contains info for selecting N for what is primarily of interest
- glmnet does not provide info for calculating Type 1/ Type II errors
  - Hmmm gonna have to think about it, he said something about looking at the estimates
  - Ones that are not significant, how often do they appear as signifcant in the 
  model
  
#### 5/4 Class Notes
- Due date for project extended to 5/13
- Do not have to turn in slides for final project
- Type I and Type II per variable per method
- TP and FP per method or variable by variable basis - valid either way
## Interim presentations
- Criteria for lasso/glmnet - use mse which is the default
- olsrr vs. ols_step_backwards
- TP and FP: Variable by variable basis or ANY of X1-X5 variables in the model
  - Going to stick with any of the 5 variables for this
    - But could consider each variable separately...
  - What proportion of the 5 variables were captured? Which method will get
  highest proportion of the 5 variables?
- Mention key questions and hypotheses in final report
- Using step() function from backwards selection
- Refit models with lm() for lasso/glmnet to get p-values and CIs
- 5 approaches
  - Backwards selection with pvals and f-test are basically the same
- Use feature/future package for parallelization
  - Might have to be careful about seed placement?

#### Outside of class: 
- I was getting confused and my bias calculations were not working using Dr. S's
code. He was calculating relative bias but in our case because are dealing with 
0s in the vector of betas, we can't calculate relative bias because that gives us
a 0 in the denominoator. Chat says to calculate raw bias for ALL variables 
 and to also calculate relative bias but just for the NON-ZERO betas.
   - Code for relative bias: 
   nonzero_idx <- which(B != 0)
  rel_bias <- (coef(final_fit)[-1][nonzero_idx] - B[nonzero_idx]) / B[nonzero_idx]
  
##### Interin Report Notes 
- I'm going to include Introduction, Methods, and Simulation sections for 
interim plan: 
  - Introduction: Review the problem and perhaps cite some papers posted or others
  - Methods: Describe model selection approaches and provide references if necessary
  - Simulation: Write on how you will simulate the data and how we are fitting
  those data. How will you summarize and present results?
**** PERHAPS include section on: 
  - Model Notation: Here give the notation for the model
  
  
  
  
  
  
  
  
  
